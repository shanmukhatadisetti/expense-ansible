groups:
  - name: expense-alerts
    rules:
      - alert: ServiceDownAlert
        expr: up == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: Server Down Alert